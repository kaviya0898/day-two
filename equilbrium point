int []array= {1, 2, 3, 7, 6};
		int leftSum=0,rightSum=0;
		for(int i=0;i<array.length;i++)
		{
			leftSum+=array[i];
		}
		for(int i=0;i<array.length;i++)
		{
			leftSum-=array[i];
			if(leftSum==rightSum)
			{
				System.out.println(array[i]);
				break;
			}
			rightSum+=array[i];
		}
		 
